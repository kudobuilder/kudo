name: size-label
on: pull_request
jobs:
  size-label:
    runs-on: ubuntu-latest
    steps:
      - name: size-label
        uses: "DanielMSchmidt/size-label-action@262f022a9f5a429adf2deb40d597de1879fd1818"
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
