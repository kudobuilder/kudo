apiVersion: kudo.k8s.io/v1alpha1
kind: Instance
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
    operator: zookeeper
  name: zk
spec:
  operatorVersion:
    name: zookeeper-1.0
    namespace: default
    type: OperatorVersions
  # Add fields here
  name: "zk"
  parameters:
    cpus: "0.3"
