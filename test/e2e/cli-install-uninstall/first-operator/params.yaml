apiVersion: kudo.dev/v1beta1
parameters:
  - name: replicas
    description: Number of replicas that should be run as part of the deployment
    default: 2
  - name: ports
    description: Ports to use for as part of the deployment
    default:
    - 80
    - 443
    type: array
