apiVersion: apps/v1
kind: Deployment
metadata:
  name: toy1-nginx-deployment
  labels:
    app: "toy"
    version: "1.0.0"
    instance: "toy1"
  annotations:
    plan: "deploy"
    step: "deploy"
    phase: "deploy"
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:1.7.9
        env:
        - name: ENVIRONMENT_VARIABLE
          value: "30"